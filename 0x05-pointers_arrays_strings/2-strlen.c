#include "main.h"
#include <string.h>
/**
 * _strlen - this function returns length of a string
 * @s: string pointer input
 * Return: returns strlen (s)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
