#include <stdio.h>
/**
 * main - 'main' entry point
 * @void:empty parameters
 * Description:"main" serves aas the entry point of a C program
 * int main (void):main fuction does not accept any parameters
 * Return:always a success (0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);/*indicates succesful execution*/
}
